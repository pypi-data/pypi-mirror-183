[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "py_postgres"
version = "0.0.2"
authors = [
  { name="Tobias Kühn", email="tobias1.kuehn@web.de" },
]
description = "Package for easier and more convenient interaction with postgres databases"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
