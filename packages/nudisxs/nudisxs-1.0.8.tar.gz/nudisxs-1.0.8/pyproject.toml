[build-system]
requires = ["setuptools>=42,<=65.5.1", "wheel",
            "scikit-build", "cmake>=3.18", "ninja", "numpy>=1.21",
            "matplotlib","vegas"]
build-backend = "setuptools.build_meta"
