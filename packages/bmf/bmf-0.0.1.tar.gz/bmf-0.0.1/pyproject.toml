[tool.pytest.ini_options]
pythonpath = [
  "."
]

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
multi_line_output = 3

[build-system]
requires = ["setuptools", "wheel", "requests"]
build-backend = "setuptools.build_meta"
