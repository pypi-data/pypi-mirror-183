[tool.poetry]
name = "netbox-data-disk"
version = "0.0.0.0.4"
description = "Netbox Disk Plugin"
authors = [
    "Tim Rhomberg <timrhomberg@hotmail.com>",
]
readme = "README.md"
packages = [{include = "netbox_data_disk"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"