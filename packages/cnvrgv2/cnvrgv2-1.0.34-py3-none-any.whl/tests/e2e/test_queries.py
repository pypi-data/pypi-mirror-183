# import shutil
# import pytest
# import os


class TestQueries:
    def test_create(self, random_name, e2e_dataset):
        # TODO: need to add elastic to the e2e tests and have commits getting indexed
        assert True
        # name = random_name(5)
        # model = e2e_dataset.queries.create(name=name, query="fake-query")
        # assert type(model) == Query
        # assert model.name == name
