[build-system]
requires = ["asyncio", "requests","hatchling"]

build-backend = "hatchling.build"

[project]
name = "playmanity"
authors = [{name = "TheLite", email = "thelitestudio0@gmail.com"}]
version = "0.1.0"
description = "Playmanity.py is a Python library for using and creating api scripts & programs with integration of playmanity services"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]