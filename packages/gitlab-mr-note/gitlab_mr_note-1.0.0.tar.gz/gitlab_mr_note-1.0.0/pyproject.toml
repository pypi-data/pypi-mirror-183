[tool.poetry]
name = "gitlab_mr_note"
version = "1.0.0"
description = ""
authors = ["Irene <irene@wn.com>"]
readme = "README.md"
packages = [{include = "gitlab_mr_note"}]

[tool.poetry.dependencies]
python = "^3.7"
python-gitlab = "^3.12.0"
click = "^8.1.3"


[tool.poetry.group.dev.dependencies]
ipython = "^7"
ipdb = "^0.13.11"

[tool.poetry.scripts]
gitlab-mr-note = "gitlab_mr_note.cli:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
