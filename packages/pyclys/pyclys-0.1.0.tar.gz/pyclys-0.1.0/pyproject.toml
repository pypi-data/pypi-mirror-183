[tool.poetry]
name = "pyclys"
version = "0.1.0"
description = "a simples clear dm"
authors = ["SundayC-137 <nicolas.lopesbis@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
