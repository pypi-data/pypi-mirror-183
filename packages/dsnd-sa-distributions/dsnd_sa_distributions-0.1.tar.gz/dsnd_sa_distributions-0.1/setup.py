from setuptools import setup

setup(name='dsnd_sa_distributions',
      version='0.1',
      description='Gaussian distributions & Binomial distributions',
      packages=['distributions'],
      zip_safe=False)
