[tool.poetry]
name = "poetry-test-pda-fga"
version = "0.1.2"
description = ""
authors = ["Bruno-Felix <balvesfelix@gmail.com>"]
readme = "README.md"
packages = [{include = "poetry_test_pda_fga"}]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.scripts]
poetry_test_pda_fga-cli = 'poetry_test_pda_fga.cli:funcao_teste'

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
