[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
skip-string-normalization = true

[tool.pyright]
include = ["hexacolors", "hexacolors.colors", "hexacolors.offline", "hexacolors.online"]
exclude = ["**/__pycache__", "build", "dist", "docs",]